export function onMouseMoveTilt(e,intensity=20){const card=e.currentTarget;const rect=card.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;const centerX=rect.width/2;const centerY=rect.height/2;const rotateX=((y-centerY)/centerY)*-intensity;const rotateY=((x-centerX)/centerX)*intensity;card.style.transform=`rotateX(${rotateX}deg) rotateY(${rotateY}deg)`}
export function onMouseLeaveTilt(e){e.currentTarget.style.transform='rotateX(0deg) rotateY(0deg)'}
